#include <stdio.h>

/**
 * main - Entry point 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int d;
	char low;

	for (d = 48; d < 58; d++)
	{
		putchar(d);
	}
	for (low = 'a'; a <= 'z'; low++)
	{
		putchar(low);
	}
	putchar('\n');
	return (0);
}

